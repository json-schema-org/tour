---
title: "Arrays"
description: "Learn how to use JSON Schema to validate arrays."
keywords: "$schema, $id, type, array, items, string, number"
---

# Arrays

In the previous step, we have learned to define an object schema with properties using the `properties` keyword.

```json highlightLineStart={4} 
{
  "name": "John Doe ",
  "age": 25,
  "skills": ["Python","Pandas"]
}
```
In the previous step, we have used the `type` keyword to define the type of the data.

In this step, we will 


<GoodToKnowBox>
  When you use json schema, you might encounter `$schema` 
  - `$schema` 
  - `$id`
</GoodToKnowBox>
